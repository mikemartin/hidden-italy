{{-- Rendered manually (livewire.inject_assets is off) so the styles get
     baked into statically cached pages — auto-injection happens after
     Statamic caches the response, so injected assets never reach the
     cache and a second livewire.js would be injected on every hit. --}}
@livewireStyles
