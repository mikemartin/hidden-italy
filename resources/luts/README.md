The Nordic HALD CLUT lives here as `hald_nordic.png` (512x512, level-8).
It is version-controlled with the code, not an author-managed asset.
Replace the file + run `php please glide:clear` + re-warm to change the look.
